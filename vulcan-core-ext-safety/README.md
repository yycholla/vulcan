<!-- generated-by: gsd-doc-writer -->
# vulcan-core-ext-safety

`vulcan-core-ext-safety` is the core Vulcan safety hook extension.

Part of the [Vulcan](../README.md) workspace.

## Build

```bash
cargo build -p vulcan-core-ext-safety
```

## Role

This crate packages safety behavior as a core extension. The root runtime also includes safety and approval hooks under `src/hooks/`.

## Testing

```bash
cargo test -p vulcan-core-ext-safety
```
