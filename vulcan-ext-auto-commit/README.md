<!-- generated-by: gsd-doc-writer -->
# vulcan-ext-auto-commit

`vulcan-ext-auto-commit` is an auto-commit extension that runs `git commit -am` on `session_end`.

Part of the [Vulcan](../README.md) workspace.

## Build

```bash
cargo build -p vulcan-ext-auto-commit
```

## Role

This crate is a concrete extension example for session lifecycle behavior and Git-oriented automation.

## Testing

```bash
cargo test -p vulcan-ext-auto-commit
```
