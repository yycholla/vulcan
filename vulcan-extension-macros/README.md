<!-- generated-by: gsd-doc-writer -->
# vulcan-extension-macros

`vulcan-extension-macros` provides procedural macros for Vulcan extension manifests.

Part of the [Vulcan](../README.md) workspace.

## Build

```bash
cargo build -p vulcan-extension-macros
```

## Role

This crate supports extension authors by generating manifest and registration glue used by the workspace extension system.

## Testing

```bash
cargo test -p vulcan-extension-macros
```
