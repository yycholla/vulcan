<!-- generated-by: gsd-doc-writer -->
# vulcan-core-ext-skills

`vulcan-core-ext-skills` is the core Vulcan skills hook extension.

Part of the [Vulcan](../README.md) workspace.

## Build

```bash
cargo build -p vulcan-core-ext-skills
```

## Role

This crate packages the skills hook as a core extension while the hook system itself lives under `src/hooks/` in the root library crate.

## Testing

```bash
cargo test -p vulcan-core-ext-skills
```
