# Daemon — Context

Long-lived background process. Hosts session lifecycle, IPC protocol, child subagent sessions, eviction, config-watch.

## Glossary

_Stub — populate via `/grill-with-docs` when area-specific terms accumulate._

## See also

- ADR-0001 (daemon-required frontends).
- ADR-0002 (shared runtime resource pool).
- ADR-0003 (extension daemon/frontend split).
- `src/gateway/CONTEXT.md` — gateway routes via daemon client.
