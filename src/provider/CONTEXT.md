# Provider — Context

OpenAI-compatible LLM provider. Both buffered (`chat`) and streaming (`chat_stream`) paths must honor every hook event — adding a new event means wiring it into both.

## Glossary

_Stub — populate via `/grill-with-docs` when area-specific terms accumulate._
