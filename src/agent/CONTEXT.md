# Agent — Context

Long-lived agent core. Owns prompt loop, message state, hook wiring, provider invocation.

## Glossary

_Stub — populate via `/grill-with-docs` when area-specific terms accumulate._

## See also

- Root `CONTEXT.md` for global vocabulary.
- `src/hooks/CONTEXT.md` — hook events fired from agent loop.
- `src/provider/CONTEXT.md` — provider trait invoked per turn.
